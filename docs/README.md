# Teststand

- `index.html` ist die führende Ansicht (BAV Prüfprotokoll Optik).
- Stammdaten werden aus `assets/data/*.json` geladen.
- Pflege der Listen (EVU, Netzteile, Baureihen) direkt im GitHub.

Hinweis:
- Abschluss (Download/Druck/Reset) erscheint erst, wenn Pflichtfelder (VTE 10) und Unterschriften vollständig sind.
